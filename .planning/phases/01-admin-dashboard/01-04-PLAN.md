---
phase: 01-admin-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/admin/templates/access.html
  - app/admin/templates/partials/access_matrix.html
  - app/admin/templates/cache.html
  - app/admin/templates/partials/cache_config.html
  - app/admin/routes.py
  - app/api/admin/controller.py
autonomous: true
requirements: [ADMIN-04, ADMIN-05]

must_haves:
  truths:
    - "Access page shows users × scopes matrix with toggle checkboxes"
    - "Toggling a scope checkbox updates the user's scopes immediately"
    - "Cache page shows per-endpoint TTL configuration"
    - "Changing a TTL value takes effect immediately without restart"
    - "Flush cache button clears cache entries"
    - "Current cache entries and remaining TTL are visible"
  artifacts:
    - path: "app/admin/templates/access.html"
      provides: "Access rights matrix page"
    - path: "app/admin/templates/cache.html"
      provides: "Cache management page"
    - path: "app/admin/templates/partials/access_matrix.html"
      provides: "Users × scopes checkbox matrix partial"
    - path: "app/admin/templates/partials/cache_config.html"
      provides: "Cache TTL configuration partial"
  key_links:
    - from: "app/admin/routes.py"
      to: "app/cache/manager.py"
      via: "calls cache_manager.set_ttl() and cache_manager.flush()"
      pattern: "cache_manager"
    - from: "app/admin/routes.py"
      to: "app/api/admin/service.py"
      via: "calls UserService for scope updates"
---

<objective>
Build the access rights matrix and cache management pages.

Purpose: Access rights = bulk scope management, Cache management = live TTL configuration per user decision D-03.
Output: Two fully functional pages for permission control and cache tuning.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</execution_context>

<context>
@app/database/models.py
@app/api/admin/service.py
@app/cache/manager.py
@app/cache/config.py
@app/admin/routes.py
@app/admin/templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build access rights matrix page</name>
  <files>app/admin/templates/access.html, app/admin/templates/partials/access_matrix.html, app/admin/routes.py</files>
  <action>
Add routes to `app/admin/routes.py`:
- `GET /admin/access` — renders access.html with all users and all scopes
- `POST /admin/access/toggle` — receives user_id + scope, toggles scope on/off for that user via UserService, returns updated access_matrix partial
- `POST /admin/access/template` — receives user_id + template_name, applies scope template, returns updated access_matrix partial

Scope templates (defined in route handler):
- "Full Access": all scopes except MASTER
- "Bitcoin Only": [BITCOIN]
- "All Data": [BITCOIN, EMAIL, IBAN, EXCHANGE_RATES]
- "Admin": [ADMIN]

Create `app/admin/templates/access.html` (extends base.html):
- Title: "Access Rights"
- Info box explaining scopes
- Template quick-apply dropdown (per user row)
- Include access_matrix partial

Create `app/admin/templates/partials/access_matrix.html`:
- Table: rows = users, columns = scopes (MASTER, ADMIN, BITCOIN, EMAIL, IBAN, EXCHANGE_RATES)
- First column: user name + email
- Each cell: checkbox styled as toggle switch (Tailwind)
- Checked if user has that scope
- Each checkbox: `hx-post="/admin/access/toggle" hx-vals='{"user_id": "...", "scope": "..."}' hx-target="#access-matrix" hx-swap="innerHTML"`
- MASTER column only editable by MASTER users (visually disabled for ADMIN)
- Cannot remove last scope (show warning)
- Last column: scope template dropdown with "Apply" button per user row
  </action>
  <verify>
    <automated>cd /Users/fabricio/workspace/external-data-gateway && ls app/admin/templates/access.html app/admin/templates/partials/access_matrix.html && echo "Access templates created OK"</automated>
  </verify>
  <done>Access rights matrix renders with toggle checkboxes. Toggling a scope updates the user immediately via HTMX. Scope templates apply bulk scopes.</done>
</task>

<task type="auto">
  <name>Task 2: Build cache management page with live TTL editing</name>
  <files>app/admin/templates/cache.html, app/admin/templates/partials/cache_config.html, app/admin/routes.py, app/api/admin/controller.py</files>
  <action>
Add admin API endpoints to `app/api/admin/controller.py`:
- `GET /admin/cache/config` — returns current TTL config from cache_manager.get_ttl_config() (JSON)
- `PUT /admin/cache/config` — accepts dict of endpoint→ttl_seconds, calls cache_manager.set_ttl() for each (MASTER scope only)
- `DELETE /admin/cache` — calls cache_manager.flush() (MASTER/ADMIN scope)
- `DELETE /admin/cache/{endpoint}` — calls cache_manager.flush(endpoint) (MASTER/ADMIN scope)
- `GET /admin/cache/stats` — returns cache_manager.get_cache_stats() (JSON)

Add routes to `app/admin/routes.py`:
- `GET /admin/cache` — renders cache.html with current TTL config + cache stats
- `POST /admin/cache/update-ttl` — receives form data with endpoint=ttl pairs, calls cache_manager.set_ttl() for each, returns updated cache_config partial
- `POST /admin/cache/flush` — calls cache_manager.flush(), returns updated cache_config partial
- `POST /admin/cache/flush/{endpoint}` — calls cache_manager.flush(endpoint), returns updated partial

Create `app/admin/templates/cache.html` (extends base.html):
- Title: "Cache Management"
- Two sections: TTL Configuration, Cache Status

Create `app/admin/templates/partials/cache_config.html`:
- TTL Configuration section:
  - Table with columns: Endpoint, Current TTL, New TTL (input), Unit selector (seconds/minutes/hours)
  - One row per endpoint from cache config
  - "Save All" button: `hx-post="/admin/cache/update-ttl" hx-target="#cache-config" hx-swap="innerHTML"`
  - Green flash notification on save (confirms "TTL updated — effective immediately")
- Cache Status section:
  - Current entries count per endpoint
  - Total memory estimate
  - "Flush All" button (red, with hx-confirm)
  - Per-endpoint "Flush" buttons
  - Auto-refresh via `hx-get="/admin/partials/cache-stats" hx-trigger="every 10s"`
  </action>
  <verify>
    <automated>cd /Users/fabricio/workspace/external-data-gateway && python -c "
from app.cache.manager import cache_manager
# Test the flow: set TTL, verify, flush
cache_manager.set_ttl('bitcoin_price', 120)
config = cache_manager.get_ttl_config()
assert config['bitcoin_price'] == 120, f'Expected 120, got {config[\"bitcoin_price\"]}'
cache_manager.set('test', 'value', 'bitcoin_price')
stats = cache_manager.get_cache_stats()
print(f'Cache stats: {stats}')
cache_manager.flush()
print('Cache management endpoints ready')
"</automated>
  </verify>
  <done>Cache page shows TTL config with editable inputs. Saving TTL takes effect immediately. Flush buttons work. Cache stats auto-refresh.</done>
</task>

</tasks>

<verification>
- Access matrix shows all users × scopes correctly
- Toggling scope checkbox updates user in database
- Cache TTL changes are live (test: change TTL, make API call, verify new TTL applies)
- Flush clears cache entries
</verification>

<success_criteria>
- Access rights matrix with toggle switches for each user×scope
- Scope templates for quick assignment
- Cache TTL editable per endpoint with immediate effect
- Cache flush per endpoint or all
- Cache status visible with entry counts
</success_criteria>

<output>
After completion, create `.planning/phases/01-admin-dashboard/01-04-SUMMARY.md`
</output>
